# Leave empty so the SPA calls /auth and /cases on the same origin.
# Local: Nuxt nitro.devProxy forwards those paths to Identity (5250) and Origination (5135).
NUXT_PUBLIC_IDENTITY_API_URL=
NUXT_PUBLIC_ORIGINATION_API_URL=
